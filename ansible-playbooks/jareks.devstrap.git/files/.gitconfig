[alias]
  st=status
  co=checkout
  cp=cherry-pick
  br=branch
  cm=commit -m
[color]
  ui=auto

[core]
	excludesfile = ~/.gitignore_global
[push]
	default = current

[include]
  path = ~/.gitconfig_local
